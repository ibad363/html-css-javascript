// //calculate volume of cylinder exercise
// let radius = parseFloat(prompt('enter the radius of a cylinder'));
// let height = parseFloat(prompt('enter the height of a cylinder'));

// let volume = Math.PI * Math.pow(radius,2) * height;

// console.log(`The Volume of a cylinder is: ${volume}`)

//calculate simple interest
let principalAmounts = parseFloat(prompt('enter the principal amount'));
let interestRate = parseFloat(prompt('enter the rate of interest'));
let time = parseFloat(prompt('enter the time in years'));

let interest = (principalAmounts * interestRate * time) /100

console.log(`the interest on the amount ${principalAmounts} is: ${interest}`.upp)
